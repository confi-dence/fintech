import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Cards = () => {
  return (
    <SafeAreaView style={{ backgroundColor: "#fff", flex: 1 }}>
      <Text>Walletcreen</Text>
    </SafeAreaView>
  );
};

export default Cards;
